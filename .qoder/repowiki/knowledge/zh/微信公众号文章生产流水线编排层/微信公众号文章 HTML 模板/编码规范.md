- 模板使用双花括号 `{{PLACEHOLDER}}` 作为 Python 字符串替换标记，由上层脚本在构建时一次性替换，而非使用 Jinja2 等模板引擎。
- 所有样式以内联 `<style>` 块置于 `<head>` 中，避免外部 CSS 依赖，确保复制粘贴到微信编辑器后样式不丢失。
- 模板整体采用绿色系主题（#7eb559 / #ebfce5 / rgb(235,252,229) 等），并通过 body 字体指定 `'Microsoft YaHei', 'PingFang SC'` 保证 Windows 环境下的中文渲染一致性。