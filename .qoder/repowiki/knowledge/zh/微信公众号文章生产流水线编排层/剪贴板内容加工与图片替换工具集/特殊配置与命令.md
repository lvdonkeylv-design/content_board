仅能在 Windows 上运行；使用前需先在剪贴板中复制目标文章内容。`tool_clipboard_maimai.py` 需确保同目录存在 `wx_content_list.txt`；`tool_clipboard_replace_images.py` 需先执行上层流程生成 `content_instance/<实例>/process/step2_table_to_image.json`，然后以 `python tool_clipboard_replace_images.py content_instance\content_YYYYMMDD_N` 传入实例目录路径。