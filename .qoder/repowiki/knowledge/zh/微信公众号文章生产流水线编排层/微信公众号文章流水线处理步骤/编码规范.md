- 每个脚本顶部包含三行固定注释块说明步骤编号、输入/输出文件路径及模板占位符，并在末尾 `if __name__ == '__main__'` 中保留可手动修改的示例路径。
- 对外部资源（JSON/HTML/图片）路径一律基于 `os.path.dirname(os.path.abspath(__file__))` 派生到同目录 `process/` 下，避免绝对路径耦合。
- LLM 调用统一封装为 `call_model(api_url, headers, max_tokens, prompt, max_retries=MAX_RETRIES)`，在 `except requests.exceptions.RequestException` 中按 `(attempt+1)*10` 秒指数退避重试。
- 大模型返回的 JSON 文本先尝试直接 `json.loads`，再去除 ````json` 代码块标记后二次解析，最后用正则提取最外层 `{...}`/`[...]` 兜底，保证鲁棒性。
- 打印日志统一使用 `[ERROR]`/`[WARN]`/`[INFO]`/`[OK]`/`[DONE]` 前缀，便于 grep 过滤关键状态。
- 对中文/emoji 终端输出使用 `_p = lambda s: s.replace('\xa0', ' ')` 或 `_safe_print` 包装，防止 GBK 编码报错中断流程。